# goreleaser.yml
# Build customization
build:
  binary: malice
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64
brew:
  github:
    owner: maliceio
    name: homebrew-tap
  folder: Formula
  dependencies:
    - git
fpm:
  formats:
    - deb
  dependencies:
    - git